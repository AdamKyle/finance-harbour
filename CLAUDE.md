@AGENTS.md

# Claude Code Notes

The canonical skill definitions live in `.agents/skills/**`.

Claude skill wrappers also exist in `.claude/skills/**` so Claude can discover matching skills. When a Claude wrapper is selected, follow the referenced `.agents/skills/<skill-name>/SKILL.md` and its supporting markdown files as the source of truth.
